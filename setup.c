/*setup.c*/
/*
#include "setup.h"

int set_default_programs(char* config_path)
{
    FILE *fp;
    int n;
    char line[255];
    fp = fopen(config_path,"r");
    while (fgets(line,255,(FILE*)fp)) {
        if (line[0] == '#')
            continue;
        else {
			n = 0;	
		}
    }
    fclose(fp);

    return 1;
}
*/
